# Phase 4 Plan: Bug Verification

**Phase:** 04-bug-verification
**Date:** 2026-08-28
**Status:** Planning

<objective>
Verify three bug fixes from the August 2026 session via static codebase analysis. Confirm each fix is correctly implemented, document pass/fail status, and provide follow-up recommendations for live verification.
</objective>

## Requirements

- BUG-01: Verify the NCSI fix — EnableActiveProbing=1 + netprofm safety-net
- BUG-02: Verify Desktop cleanup via registry key delete (no SendMessage/FindWindow)
- BUG-03: Verify LibreWolf winget install (winget availability + LibreWolf.LibreWolf package ID)

## Constraints

- No live Windows AME Wizard environment available (Linux/WSL only)
- Verification is static codebase analysis only
- No code changes expected (verification only)
- Must search entire repo for old approach remnants

## Plan: 04-01

### Objective
Verify all three bug fixes via static analysis and produce a verification report.

### Steps

1. **BUG-01: Verify NCSI fix (Section 17)**
   - Confirm `netprofm=2` is in the safety-net hashtable (line 613)
   - Confirm `EnableActiveProbing=1` registry value (line 616)
   - Confirm `NlaSvc\Parameters\Internet` key creation with `operation: add` (line 615)
   - Confirm service restart of `netprofm` and `NlaSvc` (line 617)
   - Verify no remnants of old NCSI approach

2. **BUG-02: Verify Desktop cleanup (Section 40)**
   - Confirm `!registryKey: {path: 'HKCU\Software\Microsoft\Windows\Shell\Bags\1\Desktop', operation: delete}` is present (line 1309)
   - Search entire repo for `SendMessage`, `FindWindow`, `Progman`, `SHELLDLL_DefView` — confirm all removed
   - Confirm comment documenting the design decision is present

3. **BUG-03: Verify LibreWolf winget install (Section 10)**
   - Confirm no references to `librewolf-community/browser-windows` (the broken GitHub repo)
   - Confirm `!run: {exe: 'winget', args: 'install -e --id LibreWolf.LibreWolf --silent ...', runas: currentUserElevated, option: 'browser-librewolf'}` is present (line 221)
   - Confirm `Microsoft.DesktopAppInstaller` (winget provider) is in AppX KEEP list (line 370)
   - Confirm `librewolf.png` exists in `Images/` directory
   - Confirm `browser-librewolf` toggle exists in `playbook.conf`

4. **Create 04-VERIFICATION.md** — Comprehensive verification report with pass/fail status for each check

5. **Create 04-SUMMARY.md** — Phase summary

### Expected Output

| Bug | Verification Type | Expected Status |
|-----|-------------------|-----------------|
| BUG-01 | Static (code analysis) | VERIFIED (implementation) / UNVERIFIED (runtime) |
| BUG-02 | Static (code + repo search) | VERIFIED (implementation) / UNVERIFIED (runtime) |
| BUG-03 | Static (code + cross-ref) | VERIFIED (implementation) / UNVERIFIED (runtime) |

### Notes
- This phase produces verification artifacts only — no code changes to custom.yml
- All three fixes were applied in the August 2026 session; this phase confirms they are still in place
- Phases 1-3 modifications did not touch these areas, so fixes should be intact
