# Phase 1 Plan: Network Restructuring

**Phase:** 01-network-restructuring
**Status:** Planning
**Date:** 2026-08-28

<objective>
Strip Section 29 ("NIC / NETWORK TWEAKS") of `Configuration/custom.yml` down to Vain-only changes by removing the three FSOS (FrostySecOS) TCP/IP stack registry additions. This aligns the section with the project's design decision to use only Vain NIC driver-layer tweaks for gaming latency, not FSOS TCP/IP stack tuning for throughput.
</objective>

## Requirements

- NET-01: Remove FSOS TCP/IP stack additions from Section 29 (MaxUserPort, DisableBandwidthThrottling, FastSendDatagramThreshold)
- NET-02: Keep Vain NIC driver-layer tweaks in Section 29 (PnPCapabilities=24, EEE/wake/FlowControl/InterruptModeration/RSS/SelectiveSuspend, checksum offloads, LSOv2, RSC)
- NET-03: Keep binding trims in Section 29 (ms_lldp, ms_lltdio, ms_implat, ms_rspndr)
- NET-04: Keep TCP timestamps disabled and UDP URO enabled in Section 29
- NET-05: Keep RSC enabled via Set-NetOffloadGlobalSetting in Section 29
- NET-06: Keep MMCSS NetworkThrottlingIndex=FFFFFFFF and SystemResponsiveness=0 in Section 27 (universal, not FSOS-specific)

## Constraints

- Do not modify any section other than Section 29
- Do not touch Section 17 (NCSI fix, netprofm safety-net) or Section 25 (MMCSS block)
- Preserve all Vain-specific changes in Section 29
- Keep IPv6 preserved (per section header)
- User requires approval before editing any file (per CLAUDE.md) — plan presented for sign-off

## Verification

### Must-Have Verification
1. `grep` for `MaxUserPort` in custom.yml returns no results
2. `grep` for `DisableBandwidthThrottling` in custom.yml returns no results
3. `grep` for `FastSendDatagramThreshold` in custom.yml returns no results
4. `grep` for `EnableActiveProbing` in custom.yml returns results (NCSI fix preserved in Section 17)
5. `grep` for `NetworkThrottlingIndex` in custom.yml returns results (MMCSS in Section 25 preserved)
6. `grep` for `PnCPCapabilities` or `PnPCapabilities` in custom.yml returns results (Vain NIC tweak preserved)
7. `grep` for `ms_lldp` in custom.yml returns results (binding trim preserved)
8. `grep` for `timestamps=disabled` in custom.yml returns results (TCP tuning preserved)
9. `grep` for `uro=enabled` in custom.yml returns results (UDP tuning preserved)
10. git diff shows only Section 29 lines removed (3 registryValue directives + their comments), nothing else changed

## Plans

- [ ] 01-01: Remove FSOS TCP/IP stack additions from Section 29

### Plan 01-01: Remove FSOS TCP/IP stack additions from Section 29

**Objective:** Remove the three FSOS registryValue directives and their comments from Section 29 of `Configuration/custom.yml`, update the section header comment to reflect Vain-only approach.

**Steps:**

1. **Remove `MaxUserPort` registryValue** (line 1179-1180):
   - Delete the comment `# Open full TCP ephemeral port range (FSOS)`
   - Delete the directive `- !registryValue: {path: 'HKLM\\SYSTEM\\CurrentControlSet\\Services\\Tcpip\\Parameters', value: 'MaxUserPort', type: REG_DWORD, data: '65534'}`

2. **Remove `DisableBandwidthThrottling` registryValue** (line 1181-1182):
   - Delete the comment `# LAN bandwidth throttling off (FSOS)`
   - Delete the directive `- !registryValue: {path: 'HKLM\\SYSTEM\\CurrentControlSet\\Services\\LanmanWorkstation\\Parameters', value: 'DisableBandwidthThrottling', type: REG_DWORD, data: '1'}`

3. **Remove `FastSendDatagramThreshold` registryValue** (line 1183-1184):
   - Delete the comment `# UDP datagram send-path optimization (FSOS)`
   - Delete the directive `- !registryValue: {path: 'HKLM\\SYSTEM\\CurrentControlSet\\Services\\AFD\\Parameters', value: 'FastSendDatagramThreshold', type: REG_DWORD, data: '1500'}`

4. **Update Section 29 header comment** (line 1166):
   - Change `# SECTION 29 - NIC / NETWORK TWEAKS  (Vain block + WinSux PnPCapabilities=24)` to `# SECTION 29 - NIC / NETWORK TWEAKS  (Vain-only)`

5. **Verify changes** (see Verification section above)

**Estimated task count:** 5 steps (3 removals + 1 header update + 1 verification)

## Notes

- This is a simple edit task — no research subagent needed
- The exact lines to remove are fully specified in CLAUDE.md's "Planned future change — Network section"
- No new dependencies or tools required
- The change is a pure deletion (minus header comment update) — lowest risk of introducing bugs
