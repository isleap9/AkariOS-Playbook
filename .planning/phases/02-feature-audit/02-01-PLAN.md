# Phase 2 Plan: Feature Audit

**Phase:** 02-feature-audit
**Status:** Planning
**Date:** 2026-08-28

<objective>
Produce a verified feature matrix mapping all 40 sections in `Configuration/custom.yml` to their feature toggles, actions, and dependencies. Fix two low-risk discrepancies: wire up the dead `remove-uwp-photos` toggle in Section 12, and update the stale Section 10 header comment.
</objective>

## Requirements

- AUDIT-01: Produce a verified feature matrix mapping all 40 sections in custom.yml to their feature toggles, actions, and dependencies
- AUDIT-02: Cross-check all feature toggles in custom.yml against playbook.conf `<Name>` values — confirm 1:1 match
- AUDIT-03: Audit all bundled executables in `Executables/` — verify each is referenced by at least one section in custom.yml
- AUDIT-04: Audit Images/ directory — verify each PNG corresponds to a playbook.conf `RadioButtonOption` `FileName`
- AUDIT-05: Verify all feature toggles from CLAUDE.md ("Feature toggles" list) match the actual playbook.conf options

## Constraints

- Only edit `Configuration/custom.yml` (for the two fixes: Section 10 header + Section 12 Legacy Photo Viewer entries)
- Do not modify playbook.conf or any other files
- User must approve before any edits (per CLAUDE.md — plan presented for sign-off)
- All 40 sections verified present and sequentially numbered
- No feature toggles should be added/removed (except wiring up existing `remove-uwp-photos` toggle)

## Plans

- [ ] 02-01: Produce verified feature matrix and audit report

### Plan 02-01: Produce verified feature matrix and audit report

**Objective:** Create a comprehensive audit of all 40 sections, all toggles, all executables, and all images. Fix two discrepancies: (1) wire up `remove-uwp-photos` toggle in Section 12, (2) update Section 10 header.

**Steps:**

1. **Generate feature matrix** — Create a table mapping all 40 sections to: section name, toggles used, directive types, always-on vs gated, and notes. Verify all 40 sections exist sequentially.

2. **Cross-check feature toggles** — Verify 1:1 match between all `option:` values in custom.yml and `<Name>` values in playbook.conf. Document any mismatches (structural Names like `install-browser`, `browser-page1-next`, `AkariOS` are expected and OK).

3. **Audit executables** — For each file in `Executables/`:
   - `DisableDefender.ps1` → Referenced in Section 06 (`exeDir: true`) ✓
   - `EnableDefender.ps1` → Only in comments (restoration script, not apply-time) — classified as INTENDED
   - `SetFileTypeAssociation.ps1` → Referenced in Section 35 (`exeDir: true`) ✓
   - `settimerresolutionservice.cs` → Referenced in Section 31 (`exeDir: true`) ✓
   - `start2.bin` → Referenced in Section 26 (via `Join-Path $PWD`) ✓
   - `profile.png` → Referenced in Section 36 (via `Join-Path $PWD`) ✓
   - `NoDefender.cab` → Deployed by Section 01 robocopy to `C:\Windows\`, used by DisableDefender.ps1 at runtime — classified as INDIRECT ✓
   - `ControlPanelSettings.reg` → Only in comments (content inlined in Section 26) — classified as INTENDED (dead reference)

4. **Audit Images** — Verify each PNG in `Images/` has a matching `<FileName>` in playbook.conf and vice versa. (8 images, 8 FileName refs — all match)

5. **Verify CLAUDE.md toggles** — Cross-check the 22 feature toggles listed in CLAUDE.md against playbook.conf. (All 22 match)

6. **Fix Discrepancy 1: Wire up `remove-uwp-photos` toggle** — Add Legacy Photo Viewer registry entries to Section 12, gated on `option: 'remove-uwp-photos'`:
   - Registry entries to add (standard Legacy Photo Viewer associations):
     ```yaml
     - !registryValue: {path: 'HKCR\.jpg\OpenWithProgids', value: 'PhotoViewer.JustOpen', type: REG_SZ, data: '', option: 'remove-uwp-photos'}
     - !registryValue: {path: 'HKCR\.jpeg\OpenWithProgids', value: 'PhotoViewer.JustOpen', type: REG_SZ, data: '', option: 'remove-uwp-photos'}
     - !registryValue: {path: 'HKCR\.png\OpenWithProgids', value: 'PhotoViewer.JustOpen', type: REG_SZ, data: '', option: 'remove-uwp-photos'}
     - !registryValue: {path: 'HKCR\.bmp\OpenWithProgids', value: 'PhotoViewer.JustOpen', type: REG_SZ, data: '', option: 'remove-uwp-photos'}
     - !registryValue: {path: 'HKCR\.gif\OpenWithProgids', value: 'PhotoViewer.JustOpen', type: REG_SZ, data: '', option: 'remove-uwp-photos'}
     - !registryValue: {path: 'HKCR\.tif\OpenWithProgids', value: 'PhotoViewer.JustOpen', type: REG_SZ, data: '', option: 'remove-uwp-photos'}
     - !registryValue: {path: 'HKCR\.tiff\OpenWithProgids', value: 'PhotoViewer.JustOpen', type: REG_SZ, data: '', option: 'remove-uwp-photos'}
     ```

7. **Fix Discrepancy 2: Update Section 10 header** — Change header comment from "Firefox Dev" to "LibreWolf":
   - `# SECTION 10 - BROWSERS  (Vain set: Mercury, Thorium, Brave, Firefox Dev,` → `# SECTION 10 - BROWSERS  (Vain set: Mercury, Thorium, Brave, LibreWolf,`

8. **Create AUDIT.md** — Comprehensive audit report with feature matrix table, executable audit, image audit, toggle cross-reference, and discrepancies found/fixed

9. **Verify** — Confirm all fixes applied correctly via grep and git diff

**Estimated task count:** 9 steps

## Notes

- This is primarily a documentation/audit phase with two small code fixes
- The `remove-uwp-photos` toggle fix adds actual registry entries (not just gating existing ones) — the entries were missing from Section 12
- The Section 10 header fix is a one-line comment update
- No external tools or research needed — all data gathered from codebase scouting
